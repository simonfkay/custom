#! Template Master

source "./.template_aliases.sh"
source "./.template_functions.sh"

# TODO Do I need functions to add entries to .template_aliases and
# .template_functions, as well as cull comment data from the files for the
# manual function specifically? or can i just create a generic function in
# common/meta and add add an alias/function in here, which after loading, will
# simply sub out for a call to the generic function with the correct arguments
